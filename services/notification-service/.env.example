PORT=3007
KAFKA_BROKERS=localhost:9092
# Locally, notifications are logged to stdout instead of sent via SES
NOTIFICATION_DRIVER=log
# Production values (not needed locally):
# AWS_REGION=ap-southeast-1
# SES_FROM_ADDRESS=noreply@aerolink.app
