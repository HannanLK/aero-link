replicaCount: 1

image:
  repository: "432004895948.dkr.ecr.us-east-1.amazonaws.com/aerolink-dev/checkin-service"
  tag: "latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

serviceAccount:
  create: true
  name: checkin-service
  annotations: {}  # eks.amazonaws.com/role-arn injected by Terraform/IRSA

livenessProbe:
  httpGet:
    path: /api/v1/health/live
    port: 3000
  initialDelaySeconds: 20
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /api/v1/health/ready
    port: 3000
  initialDelaySeconds: 25
  periodSeconds: 10

podDisruptionBudget:
  enabled: true
  minAvailable: 1

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70

migrations:
  enabled: false

env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: "3000"
  - name: AWS_REGION
    value: us-east-1
  - name: KAFKA_AUTH
    value: iam
  - name: LAMBDA_QR_FUNCTION_NAME
    value: aerolink-dev-qr-generator

externalSecrets:
  enabled: true
  refreshInterval: 1h
  secrets:
    - secretKey: DATABASE_URL
      remoteRef: { key: /aerolink/dev/checkin-service/db-url }
    - secretKey: KAFKA_BROKERS
      remoteRef: { key: /aerolink/dev/shared/kafka-brokers }
